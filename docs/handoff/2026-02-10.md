# AI Context Handoff

**Project**: 새로핌ON (SeraphimON)
**Date**: 2026-02-10 16:20 KST
**From**: Claude Opus 4.6
**To**: Next Session Agent
**Related Ticket/Issue**: N/A

---

## 1. Current State Summary

> Supabase 프로젝트를 시드니 → 서울 리전으로 마이그레이션 완료 후, 인프라 정리 세션.

- 애플리케이션 상태: develop 브랜치에서 정상 동작, Vercel 프리뷰 배포 활성
- 배포 상태: 서울 리전 Supabase(`saeropim-on`, `ap-northeast-2`) 프로덕션 운영 중
- 주요 변경: 시드니 리전 프로젝트(`seraphimchoir`) pause 처리 완료, CLI 토큰 갱신

---

## 2. Important Context

- **Supabase 리전 이전**: 시드니(`ap-southeast-2`) → 서울(`ap-northeast-2`)로 완전 마이그레이션됨
  - 서울 프로젝트 ID: `xkwvcebcfkmvafpsbbqj`
  - 시드니 프로젝트 ID: `vdlhfozbvcgomfgdtdvm` (paused)
- **Supabase CLI 토큰**: `~/.zshrc`의 `SUPABASE_ACCESS_TOKEN` 환경변수를 새 토큰으로 교체함
  - 이전 토큰(`sbp_189c...`)은 대시보드에서 삭제된 상태였음
  - 새 토큰(`sbp_2332...`)으로 CLI 정상 작동 확인
- **MCP vs CLI 토큰 분리**: MCP Supabase 도구는 별도 인증을 사용하므로 CLI 토큰 만료와 무관하게 작동함

---

## 3. Completed Work

- [x] 서울 리전 Supabase 프로젝트 마이그레이션 상태 확인 (45개 마이그레이션 모두 적용)
- [x] 시드니 리전 Supabase 프로젝트 pause 처리
- [x] Supabase CLI 토큰 갱신 (`~/.zshrc` 환경변수 업데이트)
- [x] playwright-cli 스킬 추가 및 핸드오프 매니페스트 커밋/푸시

세부 내용:
- 인프라: `~/.zshrc` (SUPABASE_ACCESS_TOKEN 교체)
- 코드: `.claude/skills/playwright-cli/`, `public/handoff-manifest.json`

---

## 4. Immediate Next Steps

1. **(High)** `.claude/settings.local.json`에서 평문 비밀번호/토큰 제거
   - 현재 DB 비밀번호(`Fhals304806`)와 이전 Supabase 토큰이 노출되어 있음
   - `.gitignore`에 추가하거나 민감 정보 제거 필요
2. **(Medium)** 시드니 프로젝트 완전 삭제 결정
   - 현재 paused 상태, 필요 없으면 삭제하여 정리
3. **(Medium)** 출석 관리 기능 고도화 — 2/8~9 커밋들의 후속 작업
   - 준비완료 UX, 긴급 등단 불가 처리 등 안정화
4. **(Low)** 푸시 알림 구현 (TODO 잔존)
   - `src/lib/push-notifications.ts`에 Supabase 토큰 저장 미구현

---

## 5. Decisions Made

| 결정사항 | 이유 / 근거 | 고려했으나 선택하지 않은 대안 |
|---------|------------|----------------------------|
| 시드니 프로젝트 pause (삭제 아님) | 마이그레이션 안정성 확인 후 삭제 결정, 안전장치 | 즉시 삭제 |
| CLI 토큰을 ~/.zshrc에 유지 | 기존 방식 유지, 최소 변경 | .env.local에 통합 |

---

## 6. Blockers & Open Questions

### Blockers
- 없음

### Open Questions
- ❓ 시드니 프로젝트를 언제 완전 삭제할 것인가?
  - 옵션: 1주 후 자동 / 수동 확인 후 삭제
  - 의견: 서울 프로젝트가 1-2주 안정 운영 확인 후 삭제 권장

---

## 7. Critical Files & Locations

| 파일 | 역할 | 주의사항 |
|------|------|----------|
| `~/.zshrc` | SUPABASE_ACCESS_TOKEN 환경변수 | 토큰 변경 시 여기 수정 |
| `.claude/settings.local.json` | Claude Code 로컬 권한 설정 | **비밀번호 노출 주의 — 커밋 금지** |
| `src/store/arrangement-store.ts` | 자리배치 워크플로우 핵심 로직 | 6단계 워크플로우 |
| `src/app/arrangements/[id]/page.tsx` | 자리배치 에디터 메인 페이지 | 워크플로우 단계별 렌더링 |

---

## 8. Patterns & Conventions

- Supabase 프로젝트 관리: MCP 도구(`mcp__supabase__*`) 우선, CLI(`npx supabase`) 보조
- CLI 토큰은 `~/.zshrc`의 `SUPABASE_ACCESS_TOKEN` 환경변수로 관리
- 커밋 메시지: Conventional Commits 한국어 (`feat`, `fix`, `refactor`, `chore`, `perf`)

---

## 9. Potential Gotchas

- ⚠️ `npx supabase` CLI가 `Unauthorized` 나오면 `~/.zshrc`의 `SUPABASE_ACCESS_TOKEN` 값 확인
- ⚠️ `.claude/settings.local.json`에 민감 정보가 있으므로 절대 커밋하지 말 것
- ⚠️ 시드니 프로젝트의 마지막 2개 마이그레이션(`20260202`, `20260207`)은 이름이 null로 표시됨 — 서울에서는 정상

---

## 10. Environment State

| 항목 | 값 |
|------|-----|
| **Git Branch** | `develop` |
| **Last Commit** | `357728c` - chore: playwright-cli 스킬 추가 및 핸드오프 매니페스트 갱신 |
| **Node Version** | v24.13.0 |
| **Package Manager** | npm 11.6.2 |

필수 서비스:
- Supabase 서울 리전 (`xkwvcebcfkmvafpsbbqj`) — ACTIVE_HEALTHY
- Vercel 배포 (프리뷰)

필요한 환경 변수:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_ACCESS_TOKEN` (CLI용, ~/.zshrc)

---

## 11. Testing & Quality

- Unit Tests: 미구현
- Integration / E2E: 미구현 (playwright-cli 스킬 추가됨, 향후 활용 가능)
- 타입 체크: 정상 (최근 빌드 성공)
- 린트: 정상
- 빌드: 성공

---

## 12. Documentation Updates

- ✅ 업데이트 완료: `docs/handoff/2026-02-10.md` (본 문서)
- ⏳ 추후 필요: `.claude/settings.local.json` 민감 정보 정리

---

## Session Statistics

| 항목 | 값 |
|------|-----|
| **세션 주제** | Supabase 인프라 정리 (서울 확인, 시드니 pause, CLI 토큰 갱신) |
| **커밋 수** | 1개 |

---

## Related Commits

| 해시 | 메시지 | 시간 |
|------|--------|------|
| `357728c` | chore: playwright-cli 스킬 추가 및 핸드오프 매니페스트 갱신 | 방금 |

---

_이 문서는 `/handoff` 스킬로 자동 생성되었습니다._
