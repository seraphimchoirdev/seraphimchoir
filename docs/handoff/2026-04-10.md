# AI Context Handoff

**Project**: 새로핌ON (SeraphimON)
**Date**: 2026-04-10
**Branch**: develop

---

## 1. Current State Summary

출석 관리, 그리드, 이미지 내보내기 전반에 걸친 버그를 집중적으로 수정한 날이다. 파트장 권한 검증 로직을 `user_profiles` 기반으로 변경하고, `linked_member_id`가 없는 계정의 파트 조회 fallback을 추가하여 테스트 계정 등 특수 계정에서 발생하는 오류를 해결했다. 또한 그리드의 디바운스 race condition으로 좌석 수가 불일치하는 문제와 음수 오프셋 행의 행 라벨 겹침 버그를 일반 렌더링·캡처 모드 양쪽에서 수정했다.

갤럭시 폴드 캡처 ref 분기점 수정과 좌석번호 가독성 개선도 포함되어, 지난 4월 8일 내보내기 개선에 이어 안정화가 이어졌다.

## 3. Completed Work

- [x] `linked_member_id` 없는 계정의 파트 조회 fallback 추가
- [x] 파트장 준비완료 권한 검증 `user_profiles` 기반으로 변경
- [x] `RowOffsetValue` null 타입으로 인한 빌드 에러 수정
- [x] 디바운스 race condition으로 인한 좌석 수 불일치 수정
- [x] 일반 렌더링에서 음수 오프셋 행 라벨 겹침 수정
- [x] 캡처 모드에서 음수 오프셋 행 라벨 겹침 수정
- [x] 갤럭시 폴드 캡처 ref 분기점 수정 및 좌석번호 가독성 개선

## 4. Immediate Next Steps

1. **(High)** 파트장 권한 검증 변경 후 파트장 계정으로 준비완료 기능 재검증
2. **(Medium)** 음수 오프셋 행이 있는 배치표 캡처 실기기 확인
3. **(Low)** 디바운스 타이밍 값 최적화 검토

## 5. Decisions Made

| 결정사항 | 이유 / 근거 | 대안 |
|---------|------------|------|
| 파트장 권한 검증을 `user_profiles` 기반으로 변경 | `members` 테이블 join 없이 직접 확인 가능, `linked_member_id` 미연결 계정 대응 | members 테이블 필수 연결 강제 |
| `linked_member_id` fallback 추가 | 테스트 계정 등 특수 목적 계정은 member 연결 없이 운영될 수 있음 | 연결 없는 계정 로그인 차단 |

## 9. Potential Gotchas

- ⚠️ 파트장 권한이 `user_profiles.role`로 판단되므로, 역할 부여 누락 시 파트장 기능 비활성화됨 — 계정 생성 후 역할 설정 필수
- ⚠️ 디바운스 race condition 수정은 타이밍 기반 — 극단적으로 빠른 연속 입력 시 여전히 불일치 가능성 있음
- ⚠️ 음수 오프셋 행은 캡처/일반 렌더링 두 경로가 별도 코드이므로, 향후 레이아웃 수정 시 양쪽 모두 반영 필요

## Related Commits

| 해시 | 메시지 |
|------|--------|
| 54a1425 | fix(attendance): linked_member_id 없는 계정의 파트 조회 fallback 추가 |
| 78e0bdb | fix(attendance): 파트장 준비완료 권한 검증을 user_profiles 기반으로 변경 |
| ef10fc0 | fix(build): RowOffsetValue null 타입으로 인한 빌드 에러 수정 |
| 52ea734 | fix(grid): 디바운스 race condition으로 좌석 수 불일치 수정 |
| d3bc1af | fix(seats): 일반 렌더링에서도 음수 오프셋 행의 행 라벨 겹침 수정 |
| 0120254 | fix(export): 캡처 모드에서 음수 오프셋 행의 행 라벨 겹침 수정 |
| 3684698 | fix(export): 갤럭시 폴드 캡처 ref 분기점 수정 및 좌석번호 가독성 개선 |

---
_이 문서는 `/handoff` 스킬로 자동 생성되었습니다._
