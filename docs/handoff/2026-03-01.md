# AI Context Handoff

**Project**: 새로핌ON (SeraphimON)
**Date**: 2026-03-01
**Branch**: develop

---

## 1. Current State Summary

마이그레이션 seed 파일에서 `INSERT INTO members` 구문이 실제 실행될 경우 중복 데이터가 발생하는 문제가 확인되어 해당 INSERT 구문을 비활성화(주석 처리)했다. `supabase/seed.sql`에서 테스트 대원 데이터 삽입이 관리되며, 마이그레이션 파일에서는 스키마 정의만 담당하도록 역할을 분리했다.

이번 수정으로 `npx supabase db reset` 실행 시 members 테이블에 중복 레코드가 생성되지 않는다. 기존에는 마이그레이션과 seed 양쪽에서 동일한 데이터를 INSERT하여 unique constraint 위반 오류가 발생했다.

## 3. Completed Work

- [x] 마이그레이션 파일 내 members INSERT 구문 비활성화
- [x] seed.sql과 마이그레이션 파일 간 역할 분리 확립

## 4. Immediate Next Steps

1. **(High)** `npx supabase db reset` 실행하여 중복 해소 여부 검증
2. **(Medium)** seed.sql에 누락된 테스트 대원 데이터 있으면 보완
3. **(Low)** 다른 테이블(attendances, seats 등)에도 동일 문제 있는지 점검

## 5. Decisions Made

| 결정사항 | 이유 / 근거 | 대안 |
|---------|------------|------|
| 마이그레이션 INSERT 비활성화 | 마이그레이션은 스키마, seed는 데이터 원칙 | 마이그레이션에서 INSERT 유지하고 seed에서 제거 |

## 9. Potential Gotchas

- ⚠️ seed.sql에서 members 데이터를 직접 관리하므로, 새로운 테스트 계정 추가 시 seed.sql만 수정해야 함
- ⚠️ 로컬 `db reset` 후 기존 데이터 소실됨 — 운영 DB에는 절대 `db reset` 사용 금지

## Related Commits

| 해시 | 메시지 |
|------|--------|
| 20973d9 | fix(db): 마이그레이션 seed INSERT 비활성화로 members 중복 해소 |

---
_이 문서는 `/handoff` 스킬로 자동 생성되었습니다._
