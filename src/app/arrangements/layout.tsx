/**
 * 자리배치 관리 레이아웃
 *
 * 인증 체크는 미들웨어(src/lib/supabase/middleware.ts)에서 처리합니다.
 */
export default function ArrangementsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
